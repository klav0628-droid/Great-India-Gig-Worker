(()=>{window.approveApplication=function(id){const a=applications.find(x=>x.id===id);if(!a)return;openModal(`<h2>Approve & Assign Task</h2><div class="notice">Worker: <b>${esc(a.name)}</b><br>Gig: <b>${esc(a.gig)}</b><br>Client: ${esc(a.client)}</div><input id="taskPayment" type="number" min="1" placeholder="Payment amount (₹)"><input id="taskLink" type="url" placeholder="Task / Project Link (Admin adds link)"><input id="taskMerchant" placeholder="Merchant / Client Mobile Number"><textarea id="taskDesc" placeholder="Task instructions / deliverables"></textarea><input id="taskFaq" placeholder="Project FAQ / extra instructions (optional)"><button type="button" class="btn primary" style="width:100%" onclick="saveAssignedTask('${id}')">Approve & Assign Task</button>`) };window.saveAssignedTask=function(appId){const a=applications.find(x=>x.id===appId),payment=Number($('taskPayment').value);if(!a||payment<=0){alert('Enter a valid payment amount.');return}if(tasks.some(t=>t.applicationId===appId)){alert('Task already assigned.');return}a.status='Approved';const t={id:'T'+Date.now()+Math.random(),applicationId:a.id,workerId:a.workerId,workerName:a.name,title:a.gig,client:a.client,payment,description:$('taskDesc').value.trim(),taskLink:$('taskLink').value.trim(),merchantMobile:$('taskMerchant').value.trim(),faq:$('taskFaq').value.trim(),status:'Assigned',createdAt:new Date().toLocaleString('en-IN'),renewals:0};tasks.unshift(t);setData('applications',applications);setData('tasks',tasks);logActivity(a.workerId,a.name,'Task assigned',`Admin assigned ${a.gig}. Payment ₹${payment}.`);closeModal();renderAdmin();if(currentWorker())renderWorker();alert('Task assigned successfully.');};})();